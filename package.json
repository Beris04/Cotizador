{
  "name": "toyo-foods-comercial-android",
  "version": "1.0.0",
  "private": true,
  "description": "Toyo Foods Comercial - Visitas y Cotizador - Android",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "android:init": "npm run build:web && npx cap add android && npm run android:assets && npx cap sync android",
    "android:sync": "npm run build:web && npx cap sync android",
    "android:open": "npx cap open android",
    "android:debug": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "android:assets": "npx @capacitor/assets generate --android --iconBackgroundColor '#0b4ea2' --splashBackgroundColor '#ffffff'"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/filesystem": "8.1.2",
    "@capacitor/geolocation": "8.2.0",
    "@capacitor/network": "8.0.1",
    "@capacitor/preferences": "8.0.1",
    "@capacitor/share": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "esbuild": "^0.25.0",
    "@capacitor/assets": "3.0.5"
  }
}
